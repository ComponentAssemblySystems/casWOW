<div class="page-header">
  <div class="top-page-header">
    {%- if (breadcrumbs) -%}
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <a href="{{ '/' | url }}">Dashboard</a>
        </li>
        {%- if (eleventyNavigation.level) === 1 -%}
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- elseif (eleventyNavigation.level) === 2 -%}
        <li class="breadcrumb-item">
          <a href="javascript:history.back()">{{eleventyNavigation.parent}}</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- elseif (eleventyNavigation.level) === 3 -%}
        <li class="breadcrumb-item">
          <a href="{{ eleventyNavigation.url | url }}">{{eleventyNavigation.primaryParent}}</a>
        </li>
        <li class="breadcrumb-item">
          <a href="javascript:history.back()">{{eleventyNavigation.parent}}</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- endif -%}
      </ol>
    </nav>
    {%- else -%}{%- endif -%}
  </div>
  <div class="page-header-text">
    <div class="logo-text-badge-link">
      <div class="header-subheader">
        <div class="header-label">
          <span class="h2">
            {{ title }}
          </span>
          {%- if (badge) -%}
            {%- if (badgeCount) === 2 -%}
              <span class="badge bg-secondary">
                {{ badgeTextOne }}
              </span>
              <span class="badge bg-success">
                {{ badgeTextTwo }}
              </span>
            {%- else -%}
              <span class="badge bg-secondary">
                {{ badgeText }}
              </span>
            {%- endif -%}
          {%- else -%}
          {%- endif -%}
        </div>
          {%- if (projectName) -%}
          <span class="fw-bold">
            {{ projectTitle }}
          </span>
          {%- else -%}{%- endif -%}
      </div>
    </div>
    {%- if (toolBar) -%}
    <div class="btn-toolbar d-none d-sm-block" role="toolbar" aria-label="Toolbar with button groups">
      {%- if toolbar === "btn-group" -%}
      <div class="btn-group me-2" role="group" aria-label="First group">
        <button type="button" class="btn btn-outline-secondary">1</button>
        <button type="button" class="btn btn-outline-secondary">2</button>
        <button type="button" class="btn btn-outline-secondary">3</button>
        <button type="button" class="btn btn-outline-secondary">4</button>
      </div>
      {%- endif -%}
      {%- if toolBar === "input" -%}
      <div class="input-group">
        <div class="input-group-text" id="btnGroupAddon">@</div>
        <input type="text" class="form-control" placeholder="Input group example" aria-label="Input group example" aria-describedby="btnGroupAddon">
      </div>
      {%- endif -%}
      {%- if toolBar === "buttons" -%}
      <div class="d-flex gap-2">
        {%- if buttonCount === "1" -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="d-inline-flex d-lg-none"><i class="fa-solid {{ toolbarIcon }}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ toolbarButtonOne }}"></i></span>
            <span class="d-none d-lg-inline-flex">{{ toolbarButtonOne }}</span>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownThree }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownFour }}</a></li>
          </ul>
        </div>
        {%- elseif buttonCount === "2" -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="d-inline-flex d-lg-none"><i class="fa-solid {{ toolbarIcon }}" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ toolbarButtonOne }}"></i></span>
            <span class="d-none d-lg-inline-flex">{{ toolbarButtonOne }}</span>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownThree }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownFour }}</a></li>
          </ul>
        </div>
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="d-inline-flex d-lg-none"><i class="fa-solid fa-building" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ toolbarButtonTwo }}"></i></span>
            <span class="d-none d-lg-inline-flex">{{ toolbarButtonTwo }}</span>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
          </ul>
        </div>
        {%- elseif buttonCount === "4" -%}
          <button class="btn btn-outline-secondary" type="button">
            <span class="d-none d-lg-inline-flex">{{ buttonFourOne }}</span>
            <span class="d-inline-flex d-lg-none" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ buttonFourOne }}"><i class="fa-solid fa-calendar-days"></i></span>
          </button>
          <button class="btn btn-outline-secondary" type="button">
            <span class="d-none d-lg-inline-flex">{{ buttonFourTwo }}</span>
            <span class="d-inline-flex d-lg-none" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ buttonFourTwo }}"><i class="fa-solid fa-chart-area"></i></span>
          </button>
          <button class="btn btn-outline-secondary" type="button">
            <span class="d-none d-lg-inline-flex">{{ buttonFourThree }}</span>
            <span class="d-inline-flex d-lg-none" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ buttonFourThree }}"><i class="fa-solid fa-circle-minus"></i></span>
          </button>
        {%- else -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="d-inline-flex d-lg-none" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-title="{{ toolbarButtonCalendar }}"><i class="fa-solid {{ toolbarIcon }}"></i></span>
            <span class="d-none d-lg-inline-flex">{{ toolbarButtonCalendar }}</span>
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item">{{ calendarOne }}</a></li>
            <li><a class="dropdown-item">{{ calendarTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ calendarThree }}</a></li>
            <!-- {%- if (isExpanded) -%}
            <li data-bs-toggle="collapse" data-bs-target="#applications" aria-expanded="false"><a class="dropdown-item">{{ calendarFour }}</a>
              <ul id="applications">
                <li><a class="dropdown-item" href="#">{{ expandedOne }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedTwo }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedThree }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedFour }}</a>
                </li>
              </ul>
            </li>
            {%- else -%} -->
            <li><a class="dropdown-item" href="#">{{ calendarFour }}</a></li>
            {%- endif -%}
          </ul>
        </div>
        {%- endif -%}
        <div class="dropdown">
          <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            Actions
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Export to CSV</a></li>
          </ul>
        </div>
      </div>
      {%- endif -%}
    </div>
    {%- endif -%}
  </div>
  {%- if (tabBar) -%}
  <ul class="nav">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="#">Active</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Inactive</a>
    </li>
    <li class="nav-item">
      <a class="nav-link disabled" aria-disabled="true">Complete</a>
    </li>
  </ul>
  {%- else -%}{%- endif -%}
</div>
