<div class="page-header">
  <div class="top-page-header d-none d-sm-block">
    {%- if (breadcrumbs) -%}
    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <a href="{{ '/' | url }}">Dashboard</a>
        </li>
        {%- if (eleventyNavigation.level) === 1 -%}
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- elseif (eleventyNavigation.level) === 2 -%}
        <li class="breadcrumb-item">
          <a href="javascript:history.back()">{{eleventyNavigation.parent}}</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- elseif (eleventyNavigation.level) === 3 -%}
        <li class="breadcrumb-item">
          <a href="{{ eleventyNavigation.url | url }}">{{eleventyNavigation.primaryParent}}</a>
        </li>
        <li class="breadcrumb-item">
          <a href="javascript:history.back()">{{eleventyNavigation.parent}}</a>
        </li>
        <li class="breadcrumb-item active" aria-current="page">{{ title }}</li>
        {%- endif -%}
      </ol>
    </nav>
    {%- else -%}{%- endif -%}
  </div>
  <div class="page-header-text">
    <div class="logo-text-badge-link">
      <div class="header-subheader">
        <div class="header-label">
          <span class="h2">
            {{ title }}
          </span>
          {%- if (badge) -%}
          <span class="badge bg-secondary">
            {{ badgeText }}
          </span>
          {%- else -%}{%- endif -%}
        </div>
          {%- if (projectName) -%}
          <span class="fw-bold">
            {{ projectTitle }}
          </span>
          {%- else -%}{%- endif -%}
      </div>
    </div>
    {%- if (toolBar) -%}
    <div class="btn-toolbar d-none d-sm-block" role="toolbar" aria-label="Toolbar with button groups">
      {%- if toolbar === "btn-group" -%}
      <div class="btn-group me-2" role="group" aria-label="First group">
        <button type="button" class="btn btn-outline-secondary">1</button>
        <button type="button" class="btn btn-outline-secondary">2</button>
        <button type="button" class="btn btn-outline-secondary">3</button>
        <button type="button" class="btn btn-outline-secondary">4</button>
      </div>
      {%- endif -%}
      {%- if toolBar === "input" -%}
      <div class="input-group">
        <div class="input-group-text" id="btnGroupAddon">@</div>
        <input type="text" class="form-control" placeholder="Input group example" aria-label="Input group example" aria-describedby="btnGroupAddon">
      </div>
      {%- endif -%}
      {%- if toolBar === "buttons" -%}
      <div class="d-flex gap-2">
        {%- if buttonCount === "1" -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid {{ toolbarIcon }}"></i> {{ toolbarButtonOne }}
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownThree }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownFour }}</a></li>
          </ul>
        </div>
        {%- elseif buttonCount === "2" -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid {{ toolbarIcon }}"></i> {{ toolbarButtonOne }}
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownThree }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownFour }}</a></li>
          </ul>
        </div>
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-building"></i> {{ toolbarButtonTwo }}
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ dropdownOne }}</a></li>
            <li><a class="dropdown-item" href="#">{{ dropdownTwo }}</a></li>
          </ul>
        </div>
        {%- elseif buttonCount === "4" -%}
          <button class="btn btn-outline-secondary" type="button">
            By Date Costs
          </button>
          <button class="btn btn-outline-secondary" type="button">
            Art Chart
          </button>
          <button class="btn btn-outline-secondary" type="button">
            Exclude 990s
          </button>
        {%- else -%}
        <div class="dropdown">
          <button class="btn btn-outline-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid {{ toolbarIcon }}"></i> {{ toolbarButtonCalendar }}
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item">{{ calendarOne }}</a></li>
            <li><a class="dropdown-item">{{ calendarTwo }}</a></li>
            <li><a class="dropdown-item" href="#">{{ calendarThree }}</a></li>
            <!-- {%- if (isExpanded) -%}
            <li data-bs-toggle="collapse" data-bs-target="#applications" aria-expanded="false"><a class="dropdown-item">{{ calendarFour }}</a>
              <ul id="applications">
                <li><a class="dropdown-item" href="#">{{ expandedOne }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedTwo }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedThree }}</a>
                </li>
                <li><a class="dropdown-item" href="#">{{ expandedFour }}</a>
                </li>
              </ul>
            </li>
            {%- else -%} -->
            <li><a class="dropdown-item" href="#">{{ calendarFour }}</a></li>
            {%- endif -%}
          </ul>
        </div>
        {%- endif -%}
        <div class="dropdown">
          <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            Actions
          </button>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Export to CSV</a></li>
          </ul>
        </div>
      </div>
      {%- endif -%}
    </div>
    {%- endif -%}
  </div>
  {%- if (tabBar) -%}
  <ul class="nav">
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="#">Active</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="#">Inactive</a>
    </li>
    <li class="nav-item">
      <a class="nav-link disabled" aria-disabled="true">Complete</a>
    </li>
  </ul>
  {%- else -%}{%- endif -%}
</div>
